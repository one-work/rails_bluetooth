<div class="box is-padding-less">
  <div class="box-body">
    <span class="size-7 has-border-left-primary"><%= t('bluetooth.admin.title') %></span>
  </div>
  <div class="fixed-grid has-4-cols is-shadow p-3">
    <div class="grid">
      <%= link_to({ controller: 'bluetooth/admin/devices' }, class: 'cell has-column-centered') do %>
        <div class="text-primary">
          <%= svg_tag 'bluetooth', class: 'icon is-medium' %>
        </div>
        <div class="text-grey size-7 mt-1"><%= t('bluetooth.admin.devices.index.title') %></div>
      <% end %>
    </div>
  </div>
</div>
